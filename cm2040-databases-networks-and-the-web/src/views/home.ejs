<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/main.css" />
    <title>Event Manager - Home</title>
</head>
<body class="bg-light">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-body p-5">
                        <% if (error) { %>
                            <div class="alert alert-danger" role="alert">
                                <strong>Error:</strong> <%= error.message %>
                            </div>
                        <% } %>
                        
                        <div class="text-center mb-4">
                            <% if (siteSettings) { %>
                                <h1 class="display-4 text-primary mb-3"><%= siteSettings.siteName %></h1>
                                <p class="lead text-muted"><%= siteSettings.siteDescription %></p>
                            <% } else { %>
                                <h1 class="display-4 text-primary mb-3">Event Manager</h1>
                                <p class="lead text-muted">Welcome to the Event Management System</p>
                            <% } %>
                        </div>
                        
                        <hr class="my-4">
                        
                        <div class="row g-4">
                            <!-- Organiser Section -->
                            <div class="col-md-6">
                                <div class="card h-100 border-primary">
                                    <div class="card-header bg-primary text-white">
                                        <h3 class="card-title mb-0">Organiser Section</h3>
                                    </div>
                                    <div class="card-body">
                                        <% if (isOrganiserAuthenticated) { %>
                                            <a href="/organiser" class="btn btn-primary w-100">Go to Organiser Home Page</a>
                                        <% } else { %>
                                            <a href="/auth/organiser/login" class="btn btn-outline-primary w-100">Organiser Login</a>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Attendee Section -->
                            <div class="col-md-6">
                                <div class="card h-100 border-success">
                                    <div class="card-header bg-success text-white">
                                        <h3 class="card-title mb-0">Attendee Section</h3>
                                    </div>
                                    <div class="card-body">
                                        <a href="/attendee" class="btn btn-success w-100 mb-2">Go to Attendee Home Page</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
</body>
</html>

