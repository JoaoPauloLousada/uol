# Feature extraction
- features are columns in a data frame???
- Boolean feature: columns True or False

# Formulating predictions using features
- Create specific filters based on features so it is insightful

# Linear regression
- Linear equation: mx + b = y
- Use linear regreation and find "m" and "b" so the data fits to this model
- linregress(x, y) method
- Not always it will fit. correlation closer than 0